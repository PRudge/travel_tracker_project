<h2>Bucket List Destinations</h2>
  <% @countries.each do |country| %>
  <ul>
    <li> <a href="/cities/<%= country.id %>/view"> <%= country.format_name %></a> -
       <a href="/countries/<%= country.id %>/edit">Edit</a>

        <form action="/countries/<%= country.id %>/delete" method="POST"><input type="submit" value="Delete"></form>
    </li>
  </ul>
  <% end %>


<a href="/countries/new" class="button">Add a country</a>
